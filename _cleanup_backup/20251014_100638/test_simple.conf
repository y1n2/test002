# 简单测试配置 - 非TLS
Identity = "client.example.com";
Realm = "example.com";

# 客户端端口配置
Port = 3869;
SecPort = 0;  # 禁用TLS端口
No_SCTP;
NoRelay;

# 字典加载
LoadExtension = "../build/extensions/dict_arinc839.fdx";

# 连接到服务器 - 使用非TLS端口
ConnectPeer = "192.168.37.136" { Port = 3868; };